#pragma once

namespace Qk::Chars {
    bool IsDigit(char c);

    bool IsAlpha(char c);

    bool IsAlnum(char c);

    bool IsLowerCase(char c);

    bool IsUpperCase(char c);

    char ToLowerCase(char c);

    char ToUpperCase(char c);
}